{
  "name": "almi",
  "module": "src/api/index.ts",
  "type": "module",
  "scripts": {
      "dev": "bun --watch src/api/index.ts",
      "db:seed": "bun src/data/seed.ts",
      "start:fresh": "bun run db:seed && bun run dev"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
